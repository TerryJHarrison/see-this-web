import React from 'react';
import {Container} from "semantic-ui-react";

const CollectionLayout = ({component}) => {
  return (
    <Container>
      {component}
    </Container>
  );
}

export default CollectionLayout;
